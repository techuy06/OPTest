description: Todolist login
dataSet: [
    {browserWidth: 1024, browserHeight: 768},
    {browserWidth: 800, browserHeight: 600}
]
actors:
  - actor: WEB
    segments:
      - segment: 1
        actions:
          - macro: Login
            args:
              username: $data("config").username
              password: $data("config").password

          # - description: Click on Today
          #   action: org.getopentest.selenium.Click
          #   args:
          #     locator: { xpath: "//ul[@id='top_filters']//span[text()='Today']" }

          # - description: Click on Add Task
          #   action: org.getopentest.selenium.Click
          #   args:
          #     locator: { css: li.agenda_add_task a  }

          # - description: Type something in the taskbox
          #   action: org.getopentest.selenium.Click
          #   args:
          #     locator: {css: ul.items div.DraftEditor-root}
          #     # div.public-DraftStyleDefault-block span span DraftEditor-root}
          #     # locator: {css: li.manager div.richtextinput }
          #     # text:  Open test integrates with jenkins
            
          # - description: Type something
          #   action: org.getopentest.selenium.ActionsSendKeys
          #   args:
          #     text: OpenTest integrates with jenkins

          # - description: Click Add Task
          #   action: org.getopentest.selenium.Click
          #   args:
          #     locator: { css: li.manager div.item_editor_actions button.item_editor_submit  }